import { DashboardDesignShowcase } from "@/components/sections-demo/DashboardDesignVariants";

export const metadata = {
  title: "Vtensor — Dashboard Design Variants",
};

export default function DashboardDesignVariantsPage() {
  return (
    <main className="min-h-screen">
      <DashboardDesignShowcase />
    </main>
  );
}
